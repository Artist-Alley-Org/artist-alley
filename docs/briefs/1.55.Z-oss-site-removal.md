# PARKED BRIEF — Phase 1.55.Z: Remove `site/` from the OSS repo + rewire CI

**Status:** written + parked 2026-07-11 (user: "store that in the todo list, we'll get to it when ready"). Dispatch to the coding agent when the site split's OSS-side cleanup is prioritized. Safe to run any time — the OSS→site rebuild trigger stays inert until the `SITE_DEPLOY_HOOK` secret exists.

**Prereq already met:** the private site (`mscrnt/artist-alley-site`) is live + serving www.artist-alley.org, pulling OSS docs from `dev` at build. See `[[project_site_split_and_org_move]]`.

## Goal

Delete the now-duplicate `site/` tree from the OSS repo + rewire OSS CI. Clean break, no shims. OSS-repo-only — no app code / migrations / frontend.

## Pre-audit (6 Qs)

1. Enumerate every OSS workflow referencing `site/` or the site build: `docs.yml`, `site-github-snapshot.yml`, `mdx-hazard-check.yml`, `ci.yml`, `ui-pr.yml`. Classify each: builds/deploys site (delete/strip) vs guards doc source (keep) vs path-mention (update). `grep -rn "site/" .github/workflows/`.
2. Does `ci.yml` have a site-build job or `site/**` path trigger? Remove only that; keep all app-CI jobs.
3. Does `ui-pr.yml` reference `site/`/cloudflare for the DOCS site (remove) or the APP's own CF preview (keep)? Don't break app CI.
4. Confirm `mdx-hazard-check.yml` is self-contained (standalone Python scanner over `docs/adr/**`, `docs/install/**`, `docs/roadmap.md`) — it STAYS.
5. Does a CF Pages Deploy Hook exist for the `artist-alley` project, stored as OSS secret `SITE_DEPLOY_HOOK`? If not, the rebuild-trigger references it but stays inert (note in PR body).
6. Tracked `site/` references outside workflows (README, docs/install local-dev instructions)? Update them.

## Locked decisions

1. `git rm -r site/` (48 files) — private repo is authoritative.
2. Delete `docs.yml` + `site-github-snapshot.yml`.
3. **KEEP `mdx-hazard-check.yml`** — OSS doc edits still feed the private site build; a braced-identifier in an OSS ADR breaks it. This is the one site-related check that must survive.
4. Strip site-build job / path-filter from `ci.yml` + `ui-pr.yml` surgically — do NOT touch app-CI jobs.
5. Add `.github/workflows/site-rebuild-trigger.yml`: on push to `dev` touching `docs/adr/**` | `docs/roadmap.md` | `docs/install/**` | `app/api/openapi.yaml` | `app/schema.sql` → curl `${{ secrets.SITE_DEPLOY_HOOK }}` (secret-gated, inert+logged if unset, never fails CI). Runs on `[self-hosted, artist-alley]` (OSS runner, zero hosted minutes).
6. Update README + docs/install local-dev site references (site no longer lives here). One-liner: "public docs+marketing site lives in a separate private repo, renders these docs at build."
7. Per pre-release-practices: no migration guide, no moved-stub, clean deletion.
8. `docs/` STAYS — this removes the renderer, not the source. ADRs/roadmap/openapi/schema remain (they're what the private site pulls).

## Scope OUT
App CI (Go/frontend/integration/app-Playwright/app-CF-preview), `mdx-hazard-check.yml` internals, app code, the private site repo, CF dashboard config, `docs/` content.

## Acceptance
site/ gone (48 files) · docs.yml + site-github-snapshot.yml deleted · mdx-hazard-check kept + self-contained · ci.yml + ui-pr.yml site refs surgically removed, app CI intact · site-rebuild-trigger.yml added · README/docs refs updated · docs/ source untouched · OSS CI green · roadmap 1.55.Z entry · PR body notes SITE_DEPLOY_HOOK status.

## Commits (3-4)
1. `chore(site): remove site/ — moved to private mscrnt/artist-alley-site`
2. `ci: delete docs.yml + site-github-snapshot.yml; keep mdx-hazard-check`
3. `ci: strip site refs from ci.yml + ui-pr.yml; add site-rebuild-trigger`
4. `docs: update local-dev site refs + roadmap 1.55.Z — closes #<meta>`

## Notes
- Q2+Q3 (ci.yml + ui-pr.yml surgery) are where app CI could break — read fully, remove only site bits.
- The one gotcha: don't delete mdx-hazard-check.yml (looks site-related, guards OSS doc source).
- User actions (not blocking): create CF Pages Deploy Hook for the `artist-alley` project → add as OSS secret `SITE_DEPLOY_HOOK`; optionally attach apex `artist-alley.org` to the Pages project (currently serves `www.` + `dev.`).
