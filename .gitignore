# --- Environment & secrets ---
.env
.env.*
!.env.example

# --- Local reference materials (not part of the project) ---
_docker_reference/
*.zip
*.mview

# --- ResourceSpace local state ---
# RS writes per-install secrets into include/config.php
include/config.php
# Local asset storage. Bind-mounted into containers; never committed.
filestore/
# RS-generated cache and log dirs
gfx/homeanim/
/logs/
*.log

# --- PHP / Composer ---
vendor/
.phpunit.cache/
.phpunit.result.cache
.phpcs-cache
coverage/

# --- Node / pnpm / build outputs ---
node_modules/
.svelte-kit/
dist/
build/
.vite/
.turbo/

# --- Go ---
services/**/bin/
services/**/*.exe
services/**/coverage.out

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# --- IDE / OS / Claude state ---
.vscode/
.idea/
.DS_Store
Thumbs.db
.claude/

# --- Docker / runtime ---
*.pid
.docker-data/

# --- Reference-only plugin dirs (kept locally, used as blueprints) ---
barts_grouping/
barts_annotations/
.playwright-mcp/
Reference_Plugins/

# --- Local demo seeders (reference an external dataset; kept off main) ---
scripts/seed-demo-studio.py
scripts/seed-demo-pokemon-shuffle.sql
collections-*.png
project-*.png
browse-*.png
post-*.png

# Per-format engine A/B sweep outputs (regenerated by
# scripts/blender/ab_engine_test.py).
web/static/ab/
