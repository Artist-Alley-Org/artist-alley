Hi {{.recipient_name}},

This is a test email from {{.site_name}} ({{.site_url}}).

If you're seeing this, your SMTP relay is correctly configured —
notifications, password resets, and license alerts will reach
their recipients.

Triggered by {{.triggered_by}} at {{.triggered_at}}.

--
{{.site_name}}
