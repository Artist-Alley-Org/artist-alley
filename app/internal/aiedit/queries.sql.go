// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: queries.sql

package aiedit

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getCreativeLineageByDerivative = `-- name: GetCreativeLineageByDerivative :one
SELECT derivative_asset_id, source_asset_id, generation_metadata, created_at
  FROM creative_lineage
 WHERE derivative_asset_id = $1
`

// Reverse lookup: given a derivative asset id, what's its source +
// the generation metadata that produced it. Powers the "this asset
// was generated from X with prompt Y" detail panel in E-2.
func (q *Queries) GetCreativeLineageByDerivative(ctx context.Context, derivativeAssetID pgtype.UUID) (CreativeLineage, error) {
	row := q.db.QueryRow(ctx, getCreativeLineageByDerivative, derivativeAssetID)
	var i CreativeLineage
	err := row.Scan(
		&i.DerivativeAssetID,
		&i.SourceAssetID,
		&i.GenerationMetadata,
		&i.CreatedAt,
	)
	return i, err
}

const insertCreativeLineage = `-- name: InsertCreativeLineage :one

INSERT INTO creative_lineage (
    derivative_asset_id,
    source_asset_id,
    generation_metadata
) VALUES (
    $1, $2, $3
)
RETURNING derivative_asset_id, source_asset_id, generation_metadata, created_at
`

type InsertCreativeLineageParams struct {
	DerivativeAssetID  pgtype.UUID
	SourceAssetID      pgtype.UUID
	GenerationMetadata []byte
}

// Phase 1.14.E-1 — aiedit subsystem queries.
//
// Append-only by intent (per ADR 0046 once v1.0.0 ships; pre-v1 the
// schema can still evolve). All queries here are write-once or pure
// reads — the table is the source-of-truth for "this asset was
// generated from that asset", never mutated after insert.
// Records a derivative→source link with provider-specific metadata.
// Returns the row so the caller can include created_at in its
// response without a round-trip.
func (q *Queries) InsertCreativeLineage(ctx context.Context, arg InsertCreativeLineageParams) (CreativeLineage, error) {
	row := q.db.QueryRow(ctx, insertCreativeLineage, arg.DerivativeAssetID, arg.SourceAssetID, arg.GenerationMetadata)
	var i CreativeLineage
	err := row.Scan(
		&i.DerivativeAssetID,
		&i.SourceAssetID,
		&i.GenerationMetadata,
		&i.CreatedAt,
	)
	return i, err
}

const listCreativeLineageBySource = `-- name: ListCreativeLineageBySource :many
SELECT derivative_asset_id, source_asset_id, generation_metadata, created_at
  FROM creative_lineage
 WHERE source_asset_id = $1
 ORDER BY created_at DESC
`

// Forward lookup: every derivative spawned from this source. Powers
// the "see all variations of this asset" surface in E-2 + future
// moderation flows ("show me every AI-generated child of asset X").
// Ordered newest-first so the most recent variation surfaces at the
// top of the gallery.
func (q *Queries) ListCreativeLineageBySource(ctx context.Context, sourceAssetID pgtype.UUID) ([]CreativeLineage, error) {
	rows, err := q.db.Query(ctx, listCreativeLineageBySource, sourceAssetID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CreativeLineage
	for rows.Next() {
		var i CreativeLineage
		if err := rows.Scan(
			&i.DerivativeAssetID,
			&i.SourceAssetID,
			&i.GenerationMetadata,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
