// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: queries.sql

package requests

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countPendingRequests = `-- name: CountPendingRequests :one
SELECT COUNT(*)::BIGINT FROM resource_request WHERE state = 'pending'
`

// Total count of pending rows (no capability filter — the badge
// callers post-filter or accept the unfiltered count).
func (q *Queries) CountPendingRequests(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countPendingRequests)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const getGrantsByRequestRef = `-- name: GetGrantsByRequestRef :many
SELECT user_ref, capability_code, granted_at, granted_by_user_ref,
       note, team_id, expires_at, request_ref
FROM user_capability_grants
WHERE request_ref = $1
`

// Used by the CapabilitySweeper request-cascade callback hook.
// Mirrors the existing user_capability_grants query shape.
func (q *Queries) GetGrantsByRequestRef(ctx context.Context, requestRef pgtype.UUID) ([]UserCapabilityGrant, error) {
	rows, err := q.db.Query(ctx, getGrantsByRequestRef, requestRef)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []UserCapabilityGrant
	for rows.Next() {
		var i UserCapabilityGrant
		if err := rows.Scan(
			&i.UserRef,
			&i.CapabilityCode,
			&i.GrantedAt,
			&i.GrantedByUserRef,
			&i.Note,
			&i.TeamID,
			&i.ExpiresAt,
			&i.RequestRef,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getResourceRequest = `-- name: GetResourceRequest :one
SELECT id, requester_user_ref, target_asset_id, requested_capability,
       reason, state, decided_at, decided_by_user_ref, decision_reason,
       expires_at, requested_at
FROM resource_request
WHERE id = $1
`

// Single-row read by id. Used by the decide handler to load the
// row's `state` before transitioning + by the admin viewer's
// detail page.
func (q *Queries) GetResourceRequest(ctx context.Context, id pgtype.UUID) (ResourceRequest, error) {
	row := q.db.QueryRow(ctx, getResourceRequest, id)
	var i ResourceRequest
	err := row.Scan(
		&i.ID,
		&i.RequesterUserRef,
		&i.TargetAssetID,
		&i.RequestedCapability,
		&i.Reason,
		&i.State,
		&i.DecidedAt,
		&i.DecidedByUserRef,
		&i.DecisionReason,
		&i.ExpiresAt,
		&i.RequestedAt,
	)
	return i, err
}

const insertResourceRequest = `-- name: InsertResourceRequest :one

INSERT INTO resource_request
    (requester_user_ref, target_asset_id, requested_capability, reason)
VALUES ($1, $2, $3, $4)
RETURNING id, requester_user_ref, target_asset_id, requested_capability,
          reason, state, decided_at, decided_by_user_ref, decision_reason,
          expires_at, requested_at
`

type InsertResourceRequestParams struct {
	RequesterUserRef    int64
	TargetAssetID       pgtype.UUID
	RequestedCapability string
	Reason              string
}

// Phase 1.17.E — resource_request queries.
//
// Naming convention: InsertX / GetX / ListX / MarkX
// mirrors the existing auth + users packages.
// Creates a fresh pending request. The state column defaults to
// 'pending' via the schema; we don't pass it explicitly so the
// INSERT can't accidentally bypass the default.
func (q *Queries) InsertResourceRequest(ctx context.Context, arg InsertResourceRequestParams) (ResourceRequest, error) {
	row := q.db.QueryRow(ctx, insertResourceRequest,
		arg.RequesterUserRef,
		arg.TargetAssetID,
		arg.RequestedCapability,
		arg.Reason,
	)
	var i ResourceRequest
	err := row.Scan(
		&i.ID,
		&i.RequesterUserRef,
		&i.TargetAssetID,
		&i.RequestedCapability,
		&i.Reason,
		&i.State,
		&i.DecidedAt,
		&i.DecidedByUserRef,
		&i.DecisionReason,
		&i.ExpiresAt,
		&i.RequestedAt,
	)
	return i, err
}

const listPendingRequests = `-- name: ListPendingRequests :many
SELECT id, requester_user_ref, target_asset_id, requested_capability,
       reason, state, decided_at, decided_by_user_ref, decision_reason,
       expires_at, requested_at
FROM resource_request
WHERE state = 'pending'
ORDER BY requested_at ASC
LIMIT $1
OFFSET $2
`

type ListPendingRequestsParams struct {
	Limit  int32
	Offset int32
}

// Approver-facing list (/admin/requests). Oldest pending first so
// nothing rots at the bottom of the queue. The handler filters
// the results by the approver's capability scope before returning
// — this query intentionally surfaces ALL pending so the per-row
// gate is the authoritative filter.
func (q *Queries) ListPendingRequests(ctx context.Context, arg ListPendingRequestsParams) ([]ResourceRequest, error) {
	rows, err := q.db.Query(ctx, listPendingRequests, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ResourceRequest
	for rows.Next() {
		var i ResourceRequest
		if err := rows.Scan(
			&i.ID,
			&i.RequesterUserRef,
			&i.TargetAssetID,
			&i.RequestedCapability,
			&i.Reason,
			&i.State,
			&i.DecidedAt,
			&i.DecidedByUserRef,
			&i.DecisionReason,
			&i.ExpiresAt,
			&i.RequestedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRequestsForRequester = `-- name: ListRequestsForRequester :many
SELECT id, requester_user_ref, target_asset_id, requested_capability,
       reason, state, decided_at, decided_by_user_ref, decision_reason,
       expires_at, requested_at
FROM resource_request
WHERE requester_user_ref = $1
ORDER BY requested_at DESC
LIMIT $2
`

type ListRequestsForRequesterParams struct {
	RequesterUserRef int64
	Limit            int32
}

// Requester-facing list (/account/requests). Most-recent first.
// limit is hard-capped at the handler layer (50 default; max 200).
func (q *Queries) ListRequestsForRequester(ctx context.Context, arg ListRequestsForRequesterParams) ([]ResourceRequest, error) {
	rows, err := q.db.Query(ctx, listRequestsForRequester, arg.RequesterUserRef, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ResourceRequest
	for rows.Next() {
		var i ResourceRequest
		if err := rows.Scan(
			&i.ID,
			&i.RequesterUserRef,
			&i.TargetAssetID,
			&i.RequestedCapability,
			&i.Reason,
			&i.State,
			&i.DecidedAt,
			&i.DecidedByUserRef,
			&i.DecisionReason,
			&i.ExpiresAt,
			&i.RequestedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markRequestDenied = `-- name: MarkRequestDenied :one
UPDATE resource_request
   SET state = 'denied',
       decided_at = NOW(),
       decided_by_user_ref = $2,
       decision_reason = $3
 WHERE id = $1 AND state = 'pending'
 RETURNING id, requester_user_ref, target_asset_id, requested_capability,
           reason, state, decided_at, decided_by_user_ref, decision_reason,
           expires_at, requested_at
`

type MarkRequestDeniedParams struct {
	ID               pgtype.UUID
	DecidedByUserRef *int64
	DecisionReason   string
}

// Symmetric to MarkRequestGranted.
func (q *Queries) MarkRequestDenied(ctx context.Context, arg MarkRequestDeniedParams) (ResourceRequest, error) {
	row := q.db.QueryRow(ctx, markRequestDenied, arg.ID, arg.DecidedByUserRef, arg.DecisionReason)
	var i ResourceRequest
	err := row.Scan(
		&i.ID,
		&i.RequesterUserRef,
		&i.TargetAssetID,
		&i.RequestedCapability,
		&i.Reason,
		&i.State,
		&i.DecidedAt,
		&i.DecidedByUserRef,
		&i.DecisionReason,
		&i.ExpiresAt,
		&i.RequestedAt,
	)
	return i, err
}

const markRequestExpired = `-- name: MarkRequestExpired :execrows
UPDATE resource_request
   SET state = 'expired'
 WHERE id = $1 AND state = 'granted'
`

// Called from the CapabilitySweeper request-cascade callback when
// a granted-with-request_ref user_capability_grants row reaps.
// Returns rows-affected so the sweeper logs "we expired N
// requests" — and gracefully handles the race where a sweep
// competes with a manual operator action.
func (q *Queries) MarkRequestExpired(ctx context.Context, id pgtype.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, markRequestExpired, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const markRequestGranted = `-- name: MarkRequestGranted :one
UPDATE resource_request
   SET state = 'granted',
       decided_at = NOW(),
       decided_by_user_ref = $2,
       decision_reason = $3,
       expires_at = $4
 WHERE id = $1 AND state = 'pending'
 RETURNING id, requester_user_ref, target_asset_id, requested_capability,
           reason, state, decided_at, decided_by_user_ref, decision_reason,
           expires_at, requested_at
`

type MarkRequestGrantedParams struct {
	ID               pgtype.UUID
	DecidedByUserRef *int64
	DecisionReason   string
	ExpiresAt        pgtype.Timestamptz
}

// Atomic CAS: only transitions pending → granted. Returns the
// updated row so the handler can build the audit changeset
// (1.17.D RecordChange) + the notification payload from a single
// query. Returns no rows when the request was already decided —
// the handler maps that to a 409.
func (q *Queries) MarkRequestGranted(ctx context.Context, arg MarkRequestGrantedParams) (ResourceRequest, error) {
	row := q.db.QueryRow(ctx, markRequestGranted,
		arg.ID,
		arg.DecidedByUserRef,
		arg.DecisionReason,
		arg.ExpiresAt,
	)
	var i ResourceRequest
	err := row.Scan(
		&i.ID,
		&i.RequesterUserRef,
		&i.TargetAssetID,
		&i.RequestedCapability,
		&i.Reason,
		&i.State,
		&i.DecidedAt,
		&i.DecidedByUserRef,
		&i.DecisionReason,
		&i.ExpiresAt,
		&i.RequestedAt,
	)
	return i, err
}
